import { NextPage } from 'next';
import { withInstructorLayout } from 'src/layousts/instructor';

const DraftCourses: NextPage = () => {
	return <div>DraftCoursesD</div>;
};

export default withInstructorLayout(DraftCourses);